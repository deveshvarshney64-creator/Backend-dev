# Employee Management System  

A secure Node.js + Express application with JWT-based authentication, EJS views, and protected employee routes.  
Features include user signup/login, token-based authorization, and role-safe access control.
